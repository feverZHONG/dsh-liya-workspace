# 莉娅工作区插件 —— 挂载条目
# 被 dsh.profile.bundles 按加入顺序应用（在 @deepseek-ai/dsh-base 与 dsh-web-app 之后）
# workspaceRoot 留空：优先在 WebUI 设置页「莉娅工作区」里填写（即时生效），
# 其次用本 config 兜底，最后回退到 DSH host 进程当前工作目录。
- insert:
    - id: liya-workspace
      name: liya-workspace-plugin
      config:
        workspaceRoot: ""
